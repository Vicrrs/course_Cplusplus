/*
Quando você não quer que os outros (ou você mesmo) alterem os valores das variáveis existentes, use o constpalavra-chave (isso irá declarar o variável como "constante", o que significa imutável e somente leitura)
*/

#include <iostream>
using namespace std;

int main() {
    const int minutesPerHour = 60;
    const float PI = 3.14;
}
